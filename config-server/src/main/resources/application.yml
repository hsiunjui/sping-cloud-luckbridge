server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github:com/reyshaw/spring-cloud-config:git
#          username: username
#          password: password
          basedir: E:/source/spring-cloud-config
          default-label: master
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8800/eureka
